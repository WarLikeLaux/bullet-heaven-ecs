# 📜 Контракт кода

## Основные принципы

**Strict TypeScript & Performance First**

Мы придерживаемся строгой типизации и производительности (ECS). Избегаем лишних аллокаций памяти в цикле рендера (loop).

## Запреты ❌

- `as Type` - unsafe assertion.
- `any` - строго запрещено.
- `!` - non-null assertion (используй `?.` или guard clauses).
- **Комментарии:** запрещены полностью. Ни в коде, ни в конфигах. Код самодокументируемый через именование.
- **Аллокации в loop:** создание новых объектов (`new Vector3()`) внутри `update()`/`render()` запрещено. Используй пулинг.
- **Магические числа:** все константы (скорость, урон) выноси в конфиги.
- **ООП наследование:** избегай наследования (`class Enemy extends Entity`). Используй композицию (ECS).

## Обязательно ✅

- **Strict Null Checks:** все поля компонентов должны быть типизированы.
- **Data-Oriented Design:** компоненты - это чистые данные (POD), без методов. Логика только в системах.
- **Типизация:** дженерики и интерфейсы для всех систем и запросов ECS.
- **Performance:** используй `matrixAutoUpdate = false` для статики и InstancedMesh.
- **Early Return:** guard clauses в системах для отсечки невалидных сущностей.
- **Function Declaration:** используй `function` для объявления систем и утилит (не `const = () =>`).

## Тесты

- Тестируем логику систем.
- Data-driven tests (состояние до и после).
- Mocking только для внешних API.

## Проверка

```bash
pnpm lint && pnpm typecheck
```

## См. также

- `docs/purpose.md` - архитектурный манифест
- `docs/ai/style-guide/code.md` - стиль кода
