# 🎯 Цель и Философия

Мы не просто делаем игру. Мы доказываем, что в 2026 году **UI-движки (Unity, Godot, Unreal) больше не обязательны** для создания сложных игр.

## 1. Эра Code-First Gamedev с AI

Раньше разработчики шли в Unity/Godot не потому, что любили "тыкать мышкой" в редакторе, а потому что писать движок с нуля было долго и сложно.

С приходом мощных LLM (AI) ситуация изменилась:

-   **Бойлерплейт не нужен:** AI мгновенно пишет инициализацию Three.js, настраивает сцены, камеры и свет.
-   **Математика решена:** Векторная алгебра, кватернионы, физика столкновений — AI генерирует эти алгоритмы лучше и быстрее, чем мы ищем их в документации.
-   **Шейдеры:** GLSL код пишется за секунды по текстовому описанию.

Это позволяет нам, бэкенд-разработчикам, оставаться в **нативной среде (Code Editor)**. Мы не учим интерфейс Unity, мы пишем чистый, строгий код. AI берет на себя рутину "движкописания", а мы фокусируемся на Архитектуре.

## 2. Синергия с Modern Backend (PHP 8+)

TypeScript + Three.js — это стратегический выбор для Backend-разработчика:

-   **Строгая типизация:** Мы используем TS так же строго, как PHP 8+ (Interface Segregation, Generics, DTOs).
-   **Low-Level Control:** В Unity много "магии" и скрытой логики. В Three.js мы контролируем каждый кадр, каждый байт памяти и каждый вызов рендера.
-   **Performance Mindset:** Оптимизация 5000+ объектов (пуль) показывает понимание алгоритмической сложности (O(n)), работы с памятью (GC pressure) и структур данных.

## 3. Архитектура: ECS (Entity Component System)

Мы не используем ООП (`class Bullet extends GameObject`). Это медленно и создает спагетти-код.

Мы используем **ECS** — паттерн, который по сути является **In-Memory Database**:

-   **Entities (Сущности):** ID (Primary Key).
-   **Components (Компоненты):** Данные (Столбцы таблицы).
-   **Systems (Системы):** Логика (SQL-запросы / Сервисы), обрабатывающая пачки данных.

Это "Backender's Dream": данные отделены от логики, всё чисто, предсказуемо и невероятно быстро.

## 4. Инженерный Продукт

-   **Размер:** ~300kb (Gzip) против 20MB+ у Unity WebGL.
-   **Интеграции:** Работа с SDK Яндекс Игр через типизированные интерфейсы (как с любым внешним API).
-   **Контроль:** Мы понимаем, как работает каждая строчка кода.

**Итог:** Мы используем AI, чтобы писать свой движок, а не учить чужой. Это дает уникальный опыт, глубокое понимание графики и идеальное портфолио для Full Stack инженера.
