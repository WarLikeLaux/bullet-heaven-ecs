# 🗺 Roadmap - итерации

Каждая итерация - **пощупать и проверить**. Не двигаемся дальше, пока текущая не работает.

---

## Iteration 1 - враг, который ходит за тобой

На экране появляется враг. Он идёт к игроку. Просто преследует.
Можно убегать - враг догоняет.

**Проверка:** запускаем, видим спрайт врага, он ползёт к нам.

---

## Iteration 2 - счётчик FPS

На экране отображается текущий FPS. Всегда видно, как игра себя чувствует.

**Проверка:** в углу экрана видим число FPS, оно обновляется в реальном времени.

---

## Iteration 3 - враги спавнятся

Враги появляются за пределами экрана каждые N секунд.
Со временем - чаще. Пока они просто преследуют.

**Проверка:** бегаем, вокруг появляется всё больше врагов.

---

## Iteration 4 - враги выглядят как враги

Заменяем красные квадраты на спрайты. Враги анимированные и выглядят как персонажи.

**Проверка:** на экране не квадраты, а спрайтовые существа, которые ходят за тобой.

---

## Iteration 5 - здоровье и смерть

У игрока есть HP. Враги забирают HP при касании.
Когда HP = 0 - Game Over (просто надпись).
У врагов тоже есть HP (пока не убить, но готово к следующей итерации).

⚡ **Оптимизация:** мёртвые враги удаляются из world и scene (сейчас - утечка памяти).

**Проверка:** враги подходят - HP падает - Game Over.

---

## Iteration 6 - первое оружие

Игрок автоматически стреляет проджектайлами в ближайшего врага.
Простые пули летят, попадают - враг умирает.

**Проверка:** бегаем - пули летят сами - враги дохнут.

---

## Iteration 7 - аудит: размер файлов и структура

Проверяем что файлы не раздулись, функции < 50 строк, модули маленькие.
Проверяем размер бандла. Если что-то тяжёлое - оптимизируем.

**Проверка:** `pnpm build`, размер бандла разумный. Файлы читаемые.

---

## Iteration 8 - HUD

На экране: HP-бар, таймер выживания, счётчик убийств.

**Проверка:** визуально видно состояние игры. Понятно, сколько прожил, сколько убил.

---

## Iteration 9 - опыт и левел-ап

За убийство врагов - XP. XP-бар заполняется.
При заполнении - левел-ап (пока просто анимация/звук, без выбора).

**Проверка:** убиваем врагов - полоска растёт - level up!

---

## Iteration 10 - выбор улучшения

При левел-апе - пауза, выпадают 3 карточки с улучшениями.
Выбираешь одну - игра продолжается. Улучшения реально работают
(больше урона, быстрее стрельба, больше HP).

**Проверка:** level up - выбираем карточку - чувствуем разницу.

---

## Iteration 11 - Game Over экран

Красивый экран смерти: время выживания, уровень, убийства.
Кнопка "заново". Может быть - "лучший результат".

**Проверка:** умираем - видим итоги - жмём "ещё раз".

---

## Iteration 12 - визуал: фон и снаряды

Разнообразный фон: вариации тайлов, декоративные элементы (камни, цветы).
Снаряды - спрайт или glow вместо жёлтого кружка. Trail за пулей.

**Проверка:** скриншот не стыдно показать.

---

## Iteration 13 - звуки

Стрельба, попадание, смерть врага, level up, game over.
Фоновая музыка (можно royalty-free).

**Проверка:** выключи звук - включи - совсем другое ощущение.

---

## Iteration 14 - мобильный ввод

Виртуальный джойстик / touch-управление.
Игра играбельна на телефоне.

**Проверка:** открываем на телефоне - играем пальцем.

---

## Iteration 15 - рефакторинг архитектуры

Подготовка к контентной фазе. Без этого оружия/апгрейды превратятся в switch-лапшу.

- **Weapon registry** — массив оружий у игрока, каждое с `update(world, dt)`. Вместо хардкода `runAutoFireSystem`.
- **Upgrade registry** — data-driven: массив `{ id, name, apply(player) }`. Убрать switch из `applyUpgrade`.
- **Split config** — разбить `config.ts` на `config/weapons.ts`, `config/enemies.ts`, `config/upgrades.ts`.
- **Extract game loop** — вынести `startGameLoop` из `main.ts` в `core/game.ts`. Main только бутстрапит.

**Проверка:** `main.ts` < 100 строк. Добавить новое оружие = добавить объект в массив.

---

## Iteration 16 - пауза и фокус

ESC — пауза игры с оверлеем. `visibilitychange` / `blur` — автопауза при потере фокуса вкладки.
Без этого при alt-tab `dt` копится и после возврата все враги телепортируются.

- ESC toggle `paused` + полупрозрачный оверлей "ПАУЗА".
- `document.addEventListener('visibilitychange')` → если `hidden` — пауза.
- `window.addEventListener('blur')` → пауза.
- При возврате фокуса — `lastTime = performance.now()` чтобы не было скачка dt.

**Проверка:** alt-tab, возвращаемся — враги стоят на месте, без телепорта.

---

## Iteration 17 - CI и pre-commit hook

**GitHub Actions** — автоматическая проверка на каждый push/PR:

- `pnpm install`
- `pnpm check` (format + lint + typecheck)
- `pnpm test`

Файл: `.github/workflows/ci.yml`

**Pre-commit hook** — локальная проверка перед каждым коммитом:

- `husky` — управление git hooks.
- `lint-staged` — запуск `prettier --write` и `eslint --fix` только на staged файлах.
- Блокирует коммит если lint/format не проходят.

**Проверка:** push в dev — зелёная галочка. Коммит с кривым форматом — отклоняется локально.

---

## Iteration 18 - больше оружий

3-4 типа оружий через weapon registry:

- **Orbiting** — шары вращаются вокруг игрока, бьют при контакте.
- **Area** — взрыв вокруг игрока раз в N секунд.
- **Chain** — молния перескакивает между 3-4 ближайшими врагами.

Оружия выпадают как апгрейд при level-up. У игрока может быть 2-3 одновременно.

**Проверка:** к 5-му уровню у нас зоопарк пуль и эффектов вокруг персонажа.

---

## Iteration 19 - разнообразие врагов

3-4 типа с разным поведением:

- **Runner** — быстрый, мало HP (текущий).
- **Tank** — медленный, много HP, крупный.
- **Shooter** — стреляет издалека, убегает при приближении.
- **Swarm** — мелкие, спавнятся группами по 5-8.

Каждый тип — свой спрайт (цвет/размер) и параметры в `config/enemies.ts`.

**Проверка:** появляются разные враги — тактика меняется.

---

## Iteration 20 - больше апгрейдов

Расширить пул до 15-20 апгрейдов через registry:

- Увеличение радиуса подбора (для будущего дропа).
- Регенерация HP (1 HP/сек).
- Дополнительный снаряд (multishot).
- Отражение снарядов.
- Взрыв при убийстве (chain reaction).
- Armor (снижение входящего урона).
- Магнитное поле (постоянный урон рядом).
- Удача (шанс крита ×2).

Апгрейды могут иметь уровни (1-5). При повторном выборе — усиливаются.

**Проверка:** каждый ран — уникальный билд. Два рана подряд ощущаются по-разному.

---

## Iteration 21 - дроп и магнит

Враги роняют кристаллы опыта и аптечки.
Кристаллы притягиваются к игроку с определённого расстояния (`pickupRadius`).
Апгрейд "радиус подбора" увеличивает зону магнита.

**Проверка:** экран усыпан блестяшками, подбирать приятно.

---

## Iteration 22 - волны и темп

Враги идут волнами с паузами. Каждая волна сильнее.
Между волнами — короткая передышка.
Таймер показывает прогресс. Состав волны зависит от номера (ранние — runners, поздние — танки + стрелки).

**Проверка:** чувствуется ритм: напряжение — отдых — ещё сильнее.

---

## Iteration 23 - производительность

К этому моменту: 4+ оружия, дроп на земле, разные враги — сотни объектов на экране.
Конкретный техдолг:

- ⚡ **InstancedMesh** — 1 draw call на всех врагов одного типа вместо N.
- ⚡ **Object Pool** — переиспользование геометрий, материалов, объектов.
- ⚡ **Spatial Hash** — если коллизии O(N²) тормозят, делим карту на клетки.

Подробности: `docs/ai/style-guide/optimization.md`

**Проверка:** 500+ врагов + 200+ снарядов + дроп = FPS ≥ 30.

---

## Iteration 24 - эффекты и juice

Враг умирает — вспышка/частицы.
Получаем урон — screen shake.
Level up — яркая анимация.
Числа урона всплывают над врагами.
Подбор кристаллов — звук + мини-анимация.

**Проверка:** игра перестаёт быть "техно-демкой" и начинает _ощущаться_.

---

## Iteration 25 - мини-босс

На 5-й минуте (или каждые N волн) — появляется большой враг.
У него хп-бар сверху. Много HP, уникальная атака.
При смерти — дроп гарантированного редкого улучшения.

**Проверка:** бегаем, уклоняемся, побеждаем.

---

## Iteration 26 - рывок (dash)

Space / свайп — короткий рывок в направлении движения с iframes (0.3с неуязвимости).
Кулдаун 2с. Можно давать как апгрейд при level-up.

**Проверка:** босс бьёт — уворачиваемся рывком — чувствуем себя Dark Souls.

---

## Iteration 27 - карта и границы

Мир имеет границы (невидимые стены или визуальные края).
Камера не показывает пустоту за краем мира.
Возможно: процедурная генерация зон/биомов.

**Проверка:** бегаем в край — упираемся. Мир конечен.

---

## Iteration 28 - мета-прогресс

Прогресс между сессиями (то, что держит игрока):

- **Разблокировка персонажей** — по достижениям (убей 100 врагов, выживи 5 минут).
- **Постоянные бонусы** — монеты из ранов → +% к HP/урону/скорости навсегда.
- **Статистика** — всего сыграно, рекорды, убийства за всё время.

Данные в `localStorage`.

**Проверка:** хочется играть "ещё раз" чтобы разблокировать следующего персонажа.

---

## Iteration 29 - баланс

Кривые сложности, урон оружий, HP врагов, частота спавна.
Цель: средняя сессия 10-15 минут. Не слишком легко, не безнадёжно.
Тестируем все оружия, все апгрейды, все типы врагов.

**Проверка:** играем 3-5 раз подряд — каждый раз интересно.

---

## Iteration 30 - Яндекс Игры

Интеграция с SDK. Лидерборд. Рекламные паузы (если нужны).
Публикация.

**Проверка:** заходим через Яндекс Игры — играем — видим себя в рейтинге.
